# LocalSight 198M-A64M · Ollama 模板
#
# 用法：
#   1) 把 FROM 的 GGUF 路径替换为实际文件（发布产物在 artifacts/release/）；
#   2) ollama create localsight-198m -f Modelfile
#   3) ollama run localsight-198m
#
# 思考开关：使用官方 qwen3.5 renderer（与 Qwen3 的 <think> 格式一致）。
#   - API：请求体加 "think": true / false（true 时回复带 thinking 字段）；
#   - CLI：/think 切换。
# 兼容性：系统 Ollama 0.23.2（默认端口 11434）的旧 qwen2moe runner 与
#   QK-Norm/无共享专家结构不兼容，请使用 ≥0.32 的二进制。

FROM ./localsight.Q8_0.gguf

RENDERER qwen3.5
PARSER qwen3.5

SYSTEM """你是 LocalSight，一个轻量、本地运行的中文助手。回答要准确、简洁。"""

PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER stop "<|im_end|>"
